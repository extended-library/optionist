language: node_js

node_js:
  - node

os:
  - linux
  - osx
  - windows

sudo: false

notifications:
  email: false

cache:
  npm: true

jobs:
  include:
    - stage: test
      script: npm run test

    - stage: release
      script:
        - rm -rf index.js test && mv src/* . && rm -rf src
        - ls -lah1
        - npm pack --dry-run

      deploy:
        skip_cleanup: true
        provider: script
        script:
          # - npx semantic-release
