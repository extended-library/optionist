'use strict'

const assert = require('assert')
const helpers = require('./helpers')
const optionary = helpers.getModule()

describe('Test', () => {
  it('should return true, when okay', () => {
    assert.strictEqual(true, true)
  })
})
